# Kilo AI — pointer to AGENTS.md

Session protocol lives in `AGENTS.md` at the workspace root. Read it first. Do not duplicate its rules here.

Agent roles live in `.agents/`. Activate by saying: *"Use the `<role>` role."*
