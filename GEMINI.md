# Gemini Code Assist / Antigravity Instructions (IEEPIS)

**Canonical rules live in `AGENTS.md`. Read it first.** This file only adds Gemini/Antigravity notes.

## Session start
1. Read `AGENTS.md` — session protocol, file map, token rules.
2. Follow the protocol there (handoff.md → memory.md index → optional role).
3. For Laravel/Filament tasks, also load `.ai/laravel-boost.md` (project-specific framework rules).

## Tool-specific notes
- `.aiexclude` and `.gemini/` config are respected — do not index `vendor/`, `node_modules/`, build output.
- Activate agent roles via: *"Use the `<role>` role."* — roles live in `.agents/`.

## If you are any other AI tool reading this
You are in the wrong file. Go to `AGENTS.md`.
